# Progress Log

| Date       | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-11 | Drafted spec.md, plan.md, tasks.md, and AGENTS.md with Claude from the project requirements. No code written yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-11 | Added CLAUDE.md (imports AGENTS.md) after confirming in the Claude Code docs that it doesn't read AGENTS.md directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-12 | Phase 0 scaffolding complete: backend/ (Go module, Gin boilerplate, env-based config) and frontend/ (Next.js 16, TypeScript, App Router, Tailwind) both build clean; docker-compose.yml for Postgres 16 + Mongo 7 verified healthy locally then stopped; .env.example documents all required vars.                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-12 | Phase 1 data layer complete: golang-migrate migration creates `users` (UUID PK, case-insensitive unique email scoped to active rows, password_hash/password_salt, soft delete via GORM); Mongo indexes on user_id/event/created_at for `user_logs`; UserRepository (GORM/pgx) and LogRepository (mongo-driver v2) interfaces + implementations in internal/repository. Verified against real docker-compose Postgres+Mongo: migration applies, indexes created, and a throwaway smoke test confirmed create/case-insensitive lookup/duplicate rejection/pagination/soft-delete/email-reuse-after-delete all behave correctly; smoke test deleted after use. |
| 2026-08-12 | Phase 2 auth complete: internal/auth has salted HMAC-SHA256 hashing (hmac.Equal comparison) plus a parallel VerifyAdminPassword for the config-based Admin identity (no DB row to hash against) and JWT issue/parse; AuthService.Login handles Admin and User through one flow, returning a generic ErrInvalidCredentials for unknown-email/wrong-password/wrong-admin-password so the API can't be used to enumerate emails; POST /api/v1/auth/login sets the JWT as an httpOnly, SameSite=Lax cookie, never in the body; RequireAuth/RequireAdmin middleware added for later phases. Unit tests cover hash determinism/inputs, VerifyPassword and VerifyAdminPassword table cases, and Login success/failure/unknown-email/repo-error (90% coverage on service package). Verified live against docker-compose Postgres: admin login, a real seeded user's login (incl. case-insensitive email), wrong-password and unknown-email paths, and the middleware's 401/403/200 behavior — all confirmed via curl/httptest, throwaway seed/check programs deleted after use. Deferred: wiring `user.login`/`user.login_failed` UserLog events into this handler — the async logging mechanism (buffered channel + worker) doesn't exist until Phase 3, so that wiring happens then. |
| 2026-08-12 | Phase 3 async logging complete: internal/logging.Bus wraps a buffered channel (default 256) with a non-blocking Emit(userID, event, data) that drops and warns rather than blocking the caller when full; Worker.Start drains it into LogRepository (Mongo) on its own goroutine, Worker.Stop closes the bus and blocks until fully drained for deterministic tests/shutdown. Closed the Phase 2 deferral: AuthService now takes an EventEmitter (interface, so service doesn't depend on internal/logging) and emits user.login/user.login_failed on every login outcome, including unknown-email attempts (empty user_id) and admin attempts (user_id "admin") — infra errors from the repo are not logged as login outcomes. Unit tests: Bus never blocks when full (100% coverage on internal/logging), Worker drains in order and keeps processing after a repo write error; auth_service_test.go extended to assert the correct event/user_id is emitted for every login branch. Verified live: hit /auth/login three ways (admin success, admin wrong password, unknown email) against docker-compose Mongo and confirmed all three documents landed in user_logs with correct user_id/event/data — proving the write truly happens off the request path. |
| 2026-08-12 | Phase 4 user CRUD complete: UserService (create/get/list/update/delete) hashes passwords with a fresh per-user salt on create, computes a from/to diff of only the fields that actually changed on update (password never appears — just `"password":"changed"`), and emits exactly one log event per action; a failed write (e.g. duplicate email) never emits a log event. Handlers sit behind a small `userService` interface so tests mock it instead of a real DB. Routes: GET /api/v1/users and /users/:id require any authenticated identity (RequireAuth); POST/PUT/DELETE require RequireAdmin — matching plan.md's API table. Unit tests cover service-level diff/event/error behavior (94% coverage) and handler-level validation/not-found/happy-path/duplicate-email (mocked service, no DB). Verified live end-to-end against docker-compose: admin creates a user, that user independently logs in and can list/get but gets 403 on POST/PUT, admin updates (confirmed updated_at bumped and diff logged) and soft-deletes (confirmed 404 after, and the deleted user can no longer log in — GetByEmail's soft-delete scope means that failed login logs as unknown-email, which is the correct/expected no-enumeration behavior), email reuse after delete then correctly conflicts on the second reuse attempt, invalid-id/not-found/short-password paths all returned the right status codes. Mongo user_logs showed exactly one event per action with the expected diff shape and zero password content throughout. |
| 2026-08-13 | Phase 5 frontend auth complete. Backend additions needed to support cookie-based frontend auth state (not in the original plan.md API table, added there now): GET /api/v1/auth/me (returns identity from JWT claims — added Name to the token so this needs no DB round trip) and POST /api/v1/auth/logout (clears the cookie; JWTs are stateless so there's nothing server-side to invalidate); gin-contrib/cors configured for FRONTEND_ORIGIN (new config var, default http://localhost:3000) with AllowCredentials so the httpOnly cookie survives a cross-origin fetch from the Next.js dev server. Frontend: lib/api.ts is the client-side fetch wrapper (credentials:"include"); lib/session.ts is the server-side counterpart — Server Components have no browser cookie jar, so it reads the incoming request's cookie via next/headers and forwards it explicitly to /auth/me. The (protected) route group's layout.tsx is an async Server Component that calls getSession() and redirect()s to /login before any protected UI reaches the client — auth state never touches client JS as a token, only as the already-verified identity payload. A SessionProvider/useSession context exposes that identity to client components (e.g. the logout button) without prop drilling; this will also gate admin-only UI in Phase 6. login/page.tsx is a client form posting through the API wrapper. Root page.tsx now redirects to /dashboard or /login based on session. Verified: `tsc --noEmit`, `next lint`, and `next build` all clean (both / and /dashboard correctly inferred as dynamic/server-rendered). Live end-to-end against docker-compose + both dev servers: unauthenticated requests to / and /dashboard both 307 to /login; CORS preflight and actual response both carry the correct Allow-Origin/Allow-Credentials headers; logging in against the backend and reusing that cookie against the Next.js server confirmed the full chain (cookie → getSession → backend /auth/me → embedded session payload) for both an admin and a freshly created user (role "admin" vs "user" both correct); logout cleared the cookie and /dashboard redirected again. Caveat: no browser/screenshot tool is available in this environment, so verification was via HTTP status/headers and the server-rendered payload, not a visual check in an actual browser — the dashboard is a client component, so its final DOM text only appears after React hydration, which curl can't execute; the embedded session data proves the data reached the component correctly but doesn't confirm pixel-level rendering. |
| 2026-08-13 | Phase 6 frontend user management complete. Replaced the Phase 5 placeholder dashboard with the real /users page (root and login redirects updated accordingly). @tanstack/react-table resolved to v9.1.2 — a materially different API from v8 (useTable not useReactTable, explicit tableFeatures() registration, table.FlexRender as an instance property not a standalone flexRender, columnHelper.columns([...]) needed to combine accessor+display columns without fighting TS variance on mixed TValue types) — confirmed all of this against the package's own bundled skill docs (node_modules/@tanstack/*/skills/) rather than assuming v8 knowledge applied, consistent with the "this is NOT the [version] you know" warning in frontend/AGENTS.md. Since pagination is already fully server-driven (one page of rows fetched per request), the table only uses the core row model — no pagination feature slot needed, page/pageSize/total are tracked in component state and drive refetches directly. lib/api.ts extended with listUsers/createUser/updateUser/deleteUser. UserFormModal is shared between create and edit (password optional on edit — blank means "keep current," matching the backend's UpdateUserInput semantics from Phase 4); DeleteConfirmDialog is a separate small modal. The actions column (Edit/Delete) and "New user" button are only added to the table when session.role === "admin", so a User identity gets a read-only table with no code path that could even attempt a write. Loading/empty/error states implemented (spinner-equivalent "Loading…" row, "No users yet" empty state, dismissible error banner with Retry). One eslint-disable was needed for the mount-time fetch effect (react-hooks/set-state-in-effect flags the standard fetch-on-mount pattern since the async function's first statement runs synchronously before its first await) — scoped to one line with justification, not a rule change. Verified: tsc/lint/build all clean. Live end-to-end against docker-compose: created users as admin, confirmed the exact GET /users?page=&page_size= contract the table calls; verified PUT with an omitted password preserves the old password (old password still logs in) while PUT with a password rotates it (old password then fails, new one works) — both through the literal endpoint the modal calls; DELETE removes and total count drops; a non-admin identity gets 403 on POST /users at the API layer and the /users page's embedded session payload correctly resolves role "user" for that identity (driving the frontend's own conditional rendering). Caveat carried over from Phase 5: no browser tool available, so the interactive table/modals (client-rendered after hydration) were verified by confirming the exact API contract they call behaves correctly, not by visually operating the UI in a browser. |
| 2026-08-13 | Phase 7 testing pass complete. Backend: closed the gaps left from earlier phases rather than re-testing what was already covered — internal/auth/jwt_test.go (round trip, wrong secret, expired token, and specifically a none-algorithm token via jwt.UnsafeAllowNoneSignatureType, since that's the actual attack RequireAuth's signing-method check defends against — 89.7% coverage), internal/middleware/auth_test.go (RequireAuth/RequireAdmin/ClaimsFromContext table tests, including RequireAdmin invoked without RequireAuth first — 100% coverage), and AuthHandler.Login gained a real handler-level test by introducing an `authService` interface seam mirroring the existing `userService` pattern from Phase 4 (asserts the httpOnly cookie is set with the right token on success, and that no cookie is set on invalid credentials). internal/repository/* remains untested by design — plan.md decision #6 explicitly documents mocking the repository interfaces at the service layer instead of standing up real databases for unit tests; that's what's actually exercised throughout. Full suite: 89.7-100% across auth/handler/logging/middleware/service, 0 regressions, race-clean. Frontend: had zero test infrastructure before this phase. Set up next/jest + React Testing Library per the bundled Next 16 testing guide (matched my existing Jest knowledge, no surprises). Hit two real setup snags worth remembering: (1) `@/*` path alias needs an explicit `moduleNameMapper` in jest.config.ts — next/jest does not read tsconfig paths automatically; (2) @tanstack/react-table ships ESM-only, which broke Jest until `transpilePackages` was added to next.config.ts — next/jest derives its transform-ignore list from that config value, not from a manually-added transformIgnorePatterns override (confirmed by reading next/dist/build/jest/jest.js directly — Next always builds its own ignore pattern and appends any custom one as an additional alternative, so a custom pattern can't override the default node_modules exclusion, only add another way to skip a file). Also had to stop `jest.mock("@/lib/api")` from auto-mocking the whole module, since that silently stripped the real ApiError class's constructor (instances lost their .message) — fixed by spreading jest.requireActual and only replacing the api object's methods. 26 tests across login/page, the create/edit UserFormModal, DeleteConfirmDialog, and the full UsersPage (loading/empty/error+retry states, role-gating of New user/Edit/Delete for admin vs user, opening each modal from a real row, pagination fetching page 2, logout) — 93-100% coverage on every tested file. `next build`/`next lint`/`tsc` all still clean after the transpilePackages change. |
| 2026-08-13 | Phase 8 (optional stretch) complete: GET /api/v1/users/:id/logs. Extended LogRepository with ListByUserID (Mongo Find + CountDocuments, sorted created_at desc) — deliberately does not check whether the user currently exists, since the entire point of soft delete (per spec.md Assumptions) is that historical logs stay meaningful after the user is gone; verified this live by creating/updating/soft-deleting a user and confirming all 4 events (created, updated, login_failed, deleted) were still returned newest-first after the delete. New LogService mirrors UserService's page/pageSize clamping; LogHandler sits behind Admin-only auth like the other write-adjacent routes, validates :id as a UUID (400 on garbage input) but returns an empty page rather than 404 for a well-formed id with no history. Frontend: lib/api.ts got listUserLogs; a new LogViewerModal (simple event list with created_at, event name, and a `<pre>`-formatted JSON dump of the data payload, paginated the same way as the users table) is wired to a third admin-only "Logs" action alongside Edit/Delete. Backend tests: log_service_test.go (pagination clamp, happy path, explicitly asserts a nonexistent user id returns an empty page rather than an error) and log_handler_test.go (happy path, invalid id, query param passthrough, service error) — handler coverage back up to 80.2% after dipping when this phase's code was first added untested. Frontend: 6 new tests for LogViewerModal (loading/empty/error+retry/close/pagination) plus 2 additions to the existing UsersPage suite (Logs button opens the modal and fetches the right user's history; hidden for non-admin) — 33 tests total, 93-100% coverage across all tested files, tsc/lint/build all clean. Live-verified the full authorization matrix too: non-admin gets 403, malformed id gets 400, a syntactically valid but unused UUID returns `{logs: [], total: 0}` rather than 404. |
| 2026-08-13 | Phase 9 wrap-up. Spec drift check against everything built in Phases 0-8: fixed spec.md's Users table, which still listed a single `password` field — the real schema (Phase 1) has `password_hash` + `password_salt`, per plan.md's Key Decision #1 salt addition; fixed plan.md's frontend Project Structure diagram, which showed a flat `app/`, `components/`, `lib/` layout that was never actually used — real layout is `src/app/` with a `(protected)` route group and no `components/` dir at all, since nothing ended up needing to be shared across routes (everything stayed colocated per this file's own Conventions section); added Key Decision #9 documenting the CORS/FRONTEND_ORIGIN addition from Phase 5 (wasn't a deferred decision, credentialed cookies can't use a wildcard origin per the CORS spec, so it had to be explicit from the start). Everything else in spec.md/plan.md held up against the actual implementation — no other drift found. All four Definition-of-Done items are met: Users end-to-end against Postgres, UserLogs end-to-end against Mongo written asynchronously, full unit test suite passing (backend: 89.7-100% across auth/handler/logging/middleware/service; frontend: 33 tests, 93-100% on every tested file), and spec.md/plan.md/tasks.md/AGENTS.md/progress.md all current. README.md written now that every tasks.md item is checked off (the one hard rule that gated it all project long). Final commit proposed and pending explicit approval before push, per AGENTS.md's standing rule. |
